To build the bootstrap Forth, type "make".  This will metacompile the
kernel for the C target, then call gcc to generate an executable.

The meta compiler is written in Lisp.  An attempt is made to
automatically detect an implementation.  Currently, CLISP, ECL, and
SBCL are supported.
